# Base de datos (en local usa SQLite; en Railway usa la URL de PostgreSQL)
DATABASE_URL=sqlite:///./local.db

# Opcional: si usas otro geocoding (ej. Google), añade la API key aquí
# GEOCODING_API_KEY=
